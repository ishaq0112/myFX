{
  "name": "my-fx-api",
  "version": "1.0.0",
  "description": "Your own live exchange-rate API, powered by the European Central Bank feed",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test:source": "node --env-file-if-exists=.env src/dataSource.js",
    "db:check": "node --env-file-if-exists=.env src/dbCheck.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "express": "^4.19.2"
  }
}
